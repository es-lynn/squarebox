import { ReactElement } from 'react'
export declare const SafeStorageContainer: (p: {
  children: ReactElement
  placeHolder?: ReactElement
  debug?: {
    placeHolderDelay: number
  }
}) => JSX.Element
