export const records = async (): Promise<string[]> => {
  // const posts = await DB_Posts.scan()
  // const sortedPosts = posts.sort((a, b) => b.date.getTime() - a.date.getTime())
  // return sortedPosts
  return []
}
